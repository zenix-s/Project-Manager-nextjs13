import LoginForm from "../../../components/forms/loginform"

function LoginPage() {
  return (
    <>
      <LoginForm
        onClose={() => {}}
        onSubmit={() => {}}
        title="Login"
        actionlabel="Login"
      />
    </>
  );
}
